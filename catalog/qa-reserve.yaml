# Thin in-guest catalog stub — not a dsl-work lift.
# Benchmark pointer (read-only): getafix-seed-paul dsl-work/qa_reserve_ifrs17.yaml
# Not CuPy. Not NSM math. Engines stay in panoramix-runtime bindings.

metadata:
  id: qa-reserve
  name: QA RESERVE IFRS17
  entity: RESERVE
  kind: catalog-stub
  math: false
  cupy: false
  nsm: false
  seed_file: qa_reserve_ifrs17.yaml

description: Compare data_out/RESERVE to data_expected/RESERVE

# Day-one pointer only. G3 edits overlays; runtime R3 binds engines.
