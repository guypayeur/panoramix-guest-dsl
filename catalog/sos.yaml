# Thin in-guest catalog stub — not a dsl-work lift.
# Benchmark pointer (read-only): getafix-seed-paul dsl-work/spec_sos.yaml
# Not CuPy. Not NSM math. Engines stay in panoramix-runtime bindings.

metadata:
  id: sos
  name: SOS
  entity: SOS
  kind: catalog-stub
  math: false
  cupy: false
  nsm: false
  seed_file: spec_sos.yaml

description: Stochastic-on-Stochastic (fixtures/data_in/SOS)

# Day-one pointer only. G3 edits overlays; runtime R3 binds engines.
